server:
  port: 8080
  servlet:
    context-path: /api-one

spring:
  application:
    name: postgres
  datasource:
    username: postgres
    password: Passw0rd123
    url: jdbc:postgresql://34.70.53.178:5432/postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
        format_sql: true
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

api:
  config:
#    api2URL: ${API2URL}
#    api3URL: ${API3URL}